import 'package:flutter_test/flutter_test.dart';
import 'package:annhitra_app/main.dart';

void main() {
  testWidgets('App renders', (WidgetTester tester) async {
    // Smoke test — verify app boots without error
    // Full widget tests require mocked API
  });
}
